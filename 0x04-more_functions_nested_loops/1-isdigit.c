#include "main.h"
/**
 * _isdigit - Check if a character is a digit (0-9)
 * @c: character to be checked
 *
 * Return: 1- Digit 0- Not
 */

int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{

	}	return (1);

	return (0);
}
